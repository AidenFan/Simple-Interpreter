ORIGIN IS (200, 200);
SCALE IS (100, 100/4);

ROT IS PI/4;
COLOR IS RED;
FOR T FROM 0 TO 2*PI STEP PI/50 DRAW (cos(T), sin(T));

ROT IS PI/2;
COLOR IS GREEN;
FOR T FROM 0 TO 2*PI STEP PI/50 DRAW (cos(T), sin(T));

ROT IS 3*PI/4;
COLOR IS BLUE;
FOR T FROM 0 TO 2*PI STEP PI/50 DRAW (cos(T), sin(T));
