ORIGIN IS (360, 240); // how how how
SCALE IS (100, 100); -- hhhhhhh
FOR T FROM 0 TO 2*PI STEP PI/50 DRAW (cos(T), sin(T));
SCALE IS (100, 100/3);
FOR T FROM 0 TO 2*PI STEP PI/50 DRAW (cos(T), sin(T));
ROT IS PI/2;
// dong dong dong
FOR T FROM 0 TO 2*PI STEP PI/50 DRAW (cos(T), sin(T));
